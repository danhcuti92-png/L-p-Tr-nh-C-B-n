#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double miles = 375;
    double gallons = 15;

    double mpg = miles / gallons;

    cout << fixed << setprecision(2);
    cout << "MPG = " << mpg;
    return 0;
}
